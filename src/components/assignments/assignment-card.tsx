"use client";

import Link from "next/link";
import { ArrowRight, CalendarDays, Clock3, Pencil, Trash2 } from "lucide-react";
import { formatAssignmentStatus, formatDueDate, formatPriority, getDueDateTone } from "@/lib/studyflow-format";
import type { Assignment, Course } from "@/types/studyflow";

export function AssignmentCard({ assignment, course, onEdit, onDelete }: { assignment: Assignment; course?: Course; onEdit: () => void; onDelete: (button: HTMLButtonElement) => void }) {
  const dueTone = getDueDateTone(assignment.dueDate, assignment.status);
  return <article className="flex min-w-0 flex-col rounded-[var(--radius-card)] border border-[var(--line)] bg-slate-950/40 p-5 shadow-[0_12px_28px_rgba(2,6,23,0.35)]"><div className="flex min-w-0 items-start justify-between gap-4"><div className="min-w-0"><p className="truncate text-xs font-bold uppercase tracking-[0.14em] text-cyan-300">{course?.name ?? "Course unavailable"}</p><h3 className="mt-2 break-words text-lg font-semibold tracking-tight text-white">{assignment.title}</h3></div><span className={`shrink-0 rounded-full px-2.5 py-1 text-xs font-semibold ${assignment.priority === "high" ? "bg-red-500/10 text-red-200 ring-1 ring-red-400/20" : assignment.priority === "low" ? "bg-slate-500/10 text-slate-200 ring-1 ring-slate-400/20" : "bg-amber-500/10 text-amber-200 ring-1 ring-amber-400/20"}`}>{formatPriority(assignment.priority)}</span></div><div className={`mt-4 flex flex-wrap gap-x-4 gap-y-2 text-sm ${dueTone === "overdue" ? "text-red-300" : dueTone === "today" ? "text-amber-200" : "text-slate-300"}`}><span className={`inline-flex items-center gap-1.5 ${dueTone === "overdue" ? "font-semibold text-red-300" : dueTone === "today" ? "font-semibold text-amber-200" : ""}`}><CalendarDays size={15} aria-hidden="true" />{dueTone === "overdue" ? "Overdue · " : dueTone === "today" ? "Due today · " : ""}{formatDueDate(assignment.dueDate)}</span>{assignment.estimatedMinutes && <span className="inline-flex items-center gap-1.5 text-slate-300"><Clock3 size={15} aria-hidden="true" />{assignment.estimatedMinutes} min</span>}</div><div className="mt-4"><span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-semibold ${assignment.status === "completed" ? "bg-emerald-500/10 text-emerald-200 ring-1 ring-emerald-400/20" : assignment.status === "in-progress" ? "bg-cyan-500/10 text-cyan-200 ring-1 ring-cyan-400/20" : "bg-slate-500/10 text-slate-200 ring-1 ring-slate-400/20"}`}>{formatAssignmentStatus(assignment.status)}</span></div><div className="mt-5 flex flex-wrap items-center justify-between gap-2 border-t border-[var(--line)] pt-4"><Link href={`/assignments/${assignment.id}`} className="inline-flex items-center gap-1.5 text-sm font-semibold text-cyan-300 hover:text-cyan-200 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cyan-300">Open assignment <ArrowRight size={15} aria-hidden="true" /></Link><div className="flex gap-1"><button type="button" onClick={onEdit} aria-label={`Edit ${assignment.title}`} className="inline-flex h-9 w-9 items-center justify-center rounded-md text-slate-300 transition-colors hover:bg-cyan-500/10 hover:text-cyan-200 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cyan-300"><Pencil size={16} aria-hidden="true" /></button><button type="button" onClick={(event) => onDelete(event.currentTarget)} aria-label={`Delete ${assignment.title}`} className="inline-flex h-9 w-9 items-center justify-center rounded-md text-slate-300 transition-colors hover:bg-red-500/10 hover:text-red-200 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-red-400"><Trash2 size={16} aria-hidden="true" /></button></div></div></article>;
}
